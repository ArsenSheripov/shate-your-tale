import 'package:flutter/material.dart';

const double defaultPaddingValue = 16;
const defaultPadding = EdgeInsets.symmetric(horizontal: defaultPaddingValue);
const defaultBottomPadding = EdgeInsets.only(bottom: defaultPaddingValue);
